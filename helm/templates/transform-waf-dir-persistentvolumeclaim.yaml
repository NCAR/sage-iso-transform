apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: transform-waf-dir
  name: transform-waf-dir
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: cephfs
  resources:
    requests:
      storage: 5Gi
